<!doctype html>
<html lang="en">

<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Employee Registration</title>
  <style>
    :root {
      /* 3-5 color palette */
      --primary: #0d6efd;
      /* brand blue */
      --accent: #0bc5ff;
      /* cyan accent */
      --surface: #ffffff;
      /* white */
      --muted: #e7eaee;
      /* light gray for borders */
      --text: #273043;
      /* foreground */
      --radius: 14px;
    }

    * {
      box-sizing: border-box
    }

    body {
      margin: 0;
      font-family: ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, "Helvetica Neue", Arial, "Noto Sans", "Liberation Sans", sans-serif;
      color: var(--text);
      /* gradient background to match the reference */
      background: linear-gradient(135deg, #0a4acc 0%, var(--primary) 40%, var(--accent) 100%);
      min-height: 100vh;
    }

    .sr-only {
      position: absolute;
      width: 1px;
      height: 1px;
      padding: 0;
      margin: -1px;
      overflow: hidden;
      clip: rect(0, 0, 0, 0);
      white-space: nowrap;
      border: 0;
    }

    /* Layout shell */
    .auth-shell {
      display: flex;
      align-items: stretch;
      justify-content: center;
      padding: 28px;
    }

    .auth {
      display: flex;
      width: 100%;
      max-width: 1100px;
      border-radius: var(--radius);
      overflow: hidden;
      background: transparent;
    }

    /* Left gradient panel */
    .left {
      width: 32%;
      min-width: 260px;
      padding: 40px 24px;
      color: #fff;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      gap: 20px;
      text-align: center;
    }

    .left h2 {
      margin: 0;
      font-weight: 600;
      letter-spacing: .2px;
    }

    .left small {
      opacity: .9;
      line-height: 1.6;
    }

    .rocket {
      width: 54px;
      height: 54px;
      fill: #fff;
      opacity: .95;
    }

    .btn-ghost {
      background: rgba(255, 255, 255, .25);
      border: 1px solid rgba(255, 255, 255, .55);
      color: #fff;
      padding: 10px 22px;
      border-radius: 999px;
      font-weight: 600;
      cursor: pointer;
    }

    /* Right form card */
    .right {
      flex: 1;
      background: rgba(255, 255, 255, .95);
      border-radius: 40px;
      margin-left: 24px;
      position: relative;
      padding: 38px 38px 46px;
      box-shadow:
        0 8px 20px rgba(0, 0, 0, .07),
        inset 0 0 0 1px rgba(0, 0, 0, .04);
    }

    .header {
      display: flex;
      align-items: center;
      justify-content: center;
      position: relative;
      margin-bottom: 24px;
    }

    .header h1 {
      margin: 0;
      font-size: 28px;
      font-weight: 600;
      color: #334155;
      text-align: center;
    }

    .tabs {
      position: absolute;
      right: 0;
      top: 0;
      display: flex;
      gap: 10px;
      background: #dbeafe;
      padding: 6px;
      border-radius: 999px;
    }

    .tab {
      appearance: none;
      background: transparent;
      border: none;
      padding: 8px 16px;
      border-radius: 999px;
      font-weight: 700;
      color: #0b63d6;
      cursor: pointer;
    }

    .tab.active {
      background: linear-gradient(180deg, #1488ff, #0b63d6);
      color: #fff;
      box-shadow: 0 1px 0 rgba(255, 255, 255, .35) inset;
    }

    /* Form */
    #register-form {
      display: grid;
      grid-template-columns: repeat(2, minmax(0, 1fr));
      gap: 18px 26px;
    }

    .field {
      display: flex;
      flex-direction: column;
      gap: 6px
    }

    .field input{
      padding: 10px;
      border-radius: 10px;
      font-size: 15px;
    }

    label {
      font-size: 20px;
      color: #475569
    }

    .input {
      width: 100%;
      padding: 12px 14px;
      border-radius: 10px;
      border: 1px solid var(--muted);
      background: #f7f8fa;
      color: var(--text);
      outline: none;
      transition: border-color .15s, box-shadow .15s, background .15s;
    }

    .input::placeholder {
      color: #9aa3b2
    }

    .input:focus {
      border-color: #9cc8ff;
      box-shadow: 0 0 0 3px rgba(20, 136, 255, .15);
      background: #fff;
    }

    .inline {
      display: flex;
      align-items: center;
      gap: 14px;
      height: 44px;
      padding: 0 6px;
    }

    .inline input[type="radio"] {
      accent-color: var(--primary);
    }

    .actions {
      display: flex;
      justify-content: flex-end;
      margin-top: 22px;
    }

    .btn-primary {
      background: linear-gradient(180deg, #1488ff, #0b63d6);
      color: #fff;
      border: none;
      padding: 12px 22px;
      border-radius: 999px;
      font-weight: 700;
      cursor: pointer;
      box-shadow: 0 6px 14px rgba(11, 99, 214, .25);
    }

    .btn-primary:focus {
      outline: 3px solid rgba(20, 136, 255, .3)
    }

    /* Responsive */
    @media (max-width: 980px) {
      .auth {
        flex-direction: column;
      }

      .left {
        width: 100%;
        padding: 28px;
      }

      .right {
        margin: 12px 0 0;
        border-radius: 24px;
      }

      #register-form {
        grid-template-columns: 1fr;
      }
    }
  </style>
</head>

<body>
  {{> navbar }}
  <main class="auth-shell" role="main">
    <section class="auth" aria-label="Authentication layout">
      <section class="right" aria-labelledby="form-title">
        <header class="header">
          <h1 id="form-title">Employee Registration Form</h1>
          <nav class="tabs" aria-label="Form tabs">
            <button class="tab active" type="button" aria-current="page">Registration</button>
            <button class="tab" type="button"><a href="/login" style="text-decoration: none;">Login</a></button>
          </nav>
        </header>

        <form id="register-form" action="/register" method="POST">
          <div class="field">
            <label for="firstName">First Name</label>
            <input class="form-control" id="firstName" name="firstname" type="text" placeholder="First Name *" value="" required />
          </div>

          <div class="field">
            <label for="lastName">Last Name</label>
            <input class="form-control" id="lastName" name="lastname" type="text" placeholder="Last Name *" value="" required />
          </div>

          <div class="field">
            <label for="email">Email</label>
            <input class="form-control" id="email" name="email" type="email" placeholder="Your Email *" value="" required />
          </div>

          <fieldset class="field" role="radiogroup" aria-labelledby="gender-label">
            <legend id="gender-label" class="sr-only">Gender</legend>
            <div class="inline" aria-label="Gender">
              <label><input type="radio" name="gender" value="male" checked /> Male</label>
              <label><input type="radio" name="gender" value="female" /> Female</label>
            </div>
          </fieldset>

          <div class="field">
            <label for="phone">Phone</label>
            <input class="input" id="phone" name="phone" type="tel" placeholder="Your Phone *" value="" required />
          </div>

          <div class="field">
            <label for="age">Age</label>
            <input class="input" id="age" name="age" type="number" placeholder="Enter Your Age *" min="1" max="120"
              required />
          </div>

          <div class="field">
            <label for="password">Password</label>
            <input class="input" id="password" name="password" type="password" placeholder="Password *" value="" required />
          </div>

          <div class="field">
            <label for="confirm">Confirm Password</label>
            <input class="input" id="confirm" name="cpassword" type="password" placeholder="Confirm Password *" value=""
              required />
          </div>

          <div class="actions">
          <input type="submit" class="brnRegister" value="Register" style="padding: 5px 10px; font-size: 20px; border-radius: 7px; cursor: pointer"/>
        </div>
        </form>

        
      </section>
    </section>
  </main>
</body>

</html>